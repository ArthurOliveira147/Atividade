#include<stdio.h>

int main()
{
    char nome[50];
    int i = 0;
    int quant = 0;
    
    scanf("%s",nome);
    
    while(nome[i] != '\0')
    {
        i++;
        quant++;
    }
    
    printf("A quantidade de caracteres eh: %d",quant);
    return(0);
    
}